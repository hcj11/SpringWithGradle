outercls:
  val: outer
  configurationCls:
    name: hcj
mybatis-plus:
  mapperLocations: classpath*:/mapper1/**/*.xml
  configuration:
    defaultResultSetType: forward_only
  globalConfig:
    dbConfig:
      idType: input
spring:
  profiles:
    active: local
---
info.name: externalPropertiesNames
spring:
  profiles: dev
---
servers:
  - foo.bar: aaa
  - far.boo: bbb
redis:
  port:
    - 6379
    - 8080
spring:
  profiles: local
logging:
  config: classpath:logback-test.xml