<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD com.Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="sample.mybatis.mapper.MapperInterface">
    <select id="queryWithoutParamAnnotationInXML" resultType="java.lang.String">select * from users where id=#{count} and id1=#{count1}</select>
    <select id="queryWithParamAnnotationInXML" resultType="java.lang.String">select * from users where id=#{count} and id1=#{count}</select>

</mapper>